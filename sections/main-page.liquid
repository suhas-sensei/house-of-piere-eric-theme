<article class="page" data-section-id="{{ section.id }}">
  <span class="eyebrow" style="display:block;margin-bottom:14px;">House</span>
  <h1>{{ page.title }}</h1>
  <div class="rte">{{ page.content }}</div>
</article>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
