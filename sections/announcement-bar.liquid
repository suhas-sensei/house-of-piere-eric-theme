<div class="announcement">{{ section.settings.message }}</div>

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    { "type": "text", "id": "message", "label": "Message", "default": "Complimentary discovery sample with every order" }
  ],
  "presets": [{ "name": "Announcement bar" }]
}
{% endschema %}
